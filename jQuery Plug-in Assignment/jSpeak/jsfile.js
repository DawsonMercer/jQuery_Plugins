
$(document).ready( ()=>{
    //uncomment and refresh the page to see changes
    $("body").jSpeak({
        // voiceStyle: 4,
        // voiceSpeed: 1.6,
        // textColor: "pink"
    });


});